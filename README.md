# React Project Website

Современное веб-приложение на React с TypeScript, использующее Vite для быстрой разработки и сборки.

## 🚀 Технологии

- **React 18** - библиотека для создания пользовательских интерфейсов
- **TypeScript 5.5+** - типизированный JavaScript
- **Vite** - быстрый сборщик и dev-сервер
- **React Router DOM** - маршрутизация в приложении
- **Redux Toolkit** - управление глобальным состоянием (аутентификация)
- **Axios** - HTTP-клиент для работы с API

## 📦 Структура проекта

```
src/
├── components/        # Переиспользуемые компоненты
│   ├── Navbar.tsx    # Навигационная панель
│   ├── Product.tsx   # Карточка продукта
│   ├── ProductForm.tsx
│   └── ...
├── pages/            # Страницы приложения
│   ├── Products.tsx  # Страница со списком продуктов
│   ├── Posts.tsx
│   ├── Users.tsx
│   └── Todos.tsx
├── redux/            # Redux store и slices
│   ├── store.ts      # Конфигурация store
│   └── authSlice.ts  # Slice для аутентификации
├── hooks/            # Custom hooks
│   ├── useFetch.ts   # Получение данных
│   ├── useAdd.ts     # Добавление данных
│   ├── useUpdate.ts  # Обновление данных
│   └── useDelete.ts  # Удаление данных
├── types/            # TypeScript интерфейсы
├── utils/            # Вспомогательные функции
├── modals/           # Модальные окна
└── data/             # Моковые данные

```

## 🛠 Установка и запуск

### Требования
- Node.js 18+
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение откроется на `http://localhost:5173`

### Сборка для production

```bash
npm run build
```

### Предварительный просмотр production-сборки

```bash
npm run preview
```

## ✨ Основные возможности

### 🔐 Аутентификация
- Redux-based управление состоянием входа/выхода
- Условное отображение UI-элементов на основе статуса авторизации
- Кнопки Login/Logout в навигации

### 📄 Страницы
- **Products** - список продуктов с CRUD операциями
- **Posts** - публикации
- **Users** - пользователи
- **Todos** - задачи

### 🎨 UI/UX
- Адаптивный дизайн
- Модальные окна для форм
- Debounce для поиска и фильтрации
- Плавные анимации и переходы

### 🔧 Кастомные хуки
- `useFetch` - получение данных с loading и error состояниями
- `useAdd` - добавление новых записей
- `useUpdate` - обновление существующих записей
- `useDelete` - удаление записей

## 📝 Скрипты

```bash
npm run dev      # Запуск dev-сервера
npm run build    # Сборка проекта
npm run preview  # Просмотр production-сборки
npm run lint     # Проверка кода
```

